window.SITE_CONFIG = {
  // true: usa cupos de ejemplo, simula el envío del formulario y muestra un aviso de demostración.
  // Poner en false cuando los cupos y el formulario estén conectados a datos reales.
  modoDemo: true,

  // URL que recibe la preinscripción (POST con JSON). Vacío mientras no esté conectada.
  formEndpoint: '',

  // Textos opcionales. Si quedan vacíos, el sitio oculta el dato en lugar de mostrar un valor inventado.
  fechaLimite: '',
  proximaApertura: '',
  cuposActualizado: ''
};
